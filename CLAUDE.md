# 轻待办 · 产品大脑（CLAUDE.md）

> 本文件 = 这个项目的**中央大脑**。每次会话启动自动先读它，让 Claude 带着记忆干活，不再重复调研/踩坑。
> 维护纪律：每次对话结束，把"新拍板/新教训"补进来（配 `_devlog/00` 实时日志）。用户是外行，全文讲人话。

---

## 1. 这产品是什么 / 要往哪走（一句话 + 铁目标）

- **定位**：一个"住在你设备里、帮你把随手记下的乱事变成会自动理、自动盯、会为你高兴的**个人助手**"。桌宠「轻宜」是灵魂。
- **铁目标**（用户 2026-09-04 亲口）：① 长久项目别死掉 ② 至少收回成本、能小赚更好 ③ 一年内想看到结果 ④ 网页 + 手机(PWA)都要、数据多端同步 ⑤ 面向外行，讲人话。
- **差异化种子**（非兑现）：市面上 todo/笔记/AI陪伴都各自成熟，但"一个会**记住你、主动回来盯你、陪你做事**的桌宠助手"没人做。todo 都不主动、笔记更不会。**轻宜 = 这个差异点。**
- 商业化路线见 `_devlog/04`（P0~P4，P3 留存验证是 go/no-go，别在没人持续用前做付费）。

## 2. 用户反复强调的教训（每条都是血泪，别重犯）

1. **答复前先逐问**：一次一个，追到 95% 理解再给方案。别让他选产品方向，他是外行——**给判断**。
2. **别埋头闷改**：大块功能/UI 改版前，先去 GitHub/网上看同类产品**真实做法**再动手；改 UI 尤其要先看真实截图/布局，**绝不瞎调 CSS**（他多次被"越改越垃圾"气到）。
3. **别让用户在"记录时"做选择**：先收下再说，要做的再转待办。逼人选分类 = 杀死记录习惯（flomo 调研核心）。
4. **别挤牙膏**：不要等用户一条条喂；要有产品主动性和判断，主动提"建议做 X 吗"。
5. **日志实时更新**：每次聊完自动更新 `_devlog/00` + 桌面副本 + 推 GitHub，不等用户说。
6. **调研要有后续**：调研完列"同类好在哪 → 建议做 X 吗 → 怎么做"，别丢笔记完事。
7. **安全红线**：数据串号/明文是大事；登出清空一切内存数据；注册/登录新账号绝不从残留取数。情绪危机词(自杀/自残)要本地硬拦截给热线，绝不当心理医生。

## 3. 已拍板决策（记死，别推翻重来）

- **备忘录 & 随手记**：用户困惑二者是否分开。倾向**分开更清晰**，但 UI 调研(等 agent)定夺；若主流不分，则做成"混着也自然好看"(短句有内容感)。
- **随手记收件箱**：备忘录顶部常驻输入框回车成卡；卡可「→转为待办」(NLP 解析时间→建任务→删原备忘，原文存任务备注)。
- **待办/备忘/随手记三件事的用户口径**：待办=要做的、会过期；备忘=正式的、要留存的资料(客户电话/生日/报销)；随手记=更随便的灵感/杂事/口信。**桌宠需能区分这些意图**(部分待做)。
- **编辑器**：从右侧抽屉改**居中弹窗**(用户明确"右边展开别扭")。
- **情绪陪伴**：先共情→询问"要不要记成 #心情"→确认才记(两段式，用户拍板)。
- **AI 时间**：必须把真实当前时间喂给 AI(它没时钟会自己编)。
- **登出/换账号**：resetAllLocalData 清空全部 8 个数据 state。

## 4. 当前 🔴 未解决（最高优先到低）

1. **备忘录/随手记 UI**：用户否了窄列居中("只是变短条，没意义")+ 仍分不清二者。**正在查 memos/flomo 真实 UI**(agent 进行中)。据实重做 或 决定分开。← 现在最高优先。
2. **刷新/打开很慢**：未查根因(怀疑=刷新即重新登录+拉全量+解密)。UI 定了查。
3. **桌宠分不清随手记/任务/目标等类别**：只做了收件箱，类别识别/目标联动未做。
4. 调研建议待拍板(B 档)：完成仪式(打勾音效+点亮+鼓励)/今日"轻宜建议"栏/主动跟进/带记忆问候/flomo回顾(随机翻旧卡)/亲密度分档/NLP高亮chips/晚间回顾/危机词硬gate(安全,建议单独立项)。

## 5. 技术速览（别迷路）

- 栈：React19+TS+Vite7 → Cloudflare Workers+D1；E2EE(AES-GCM,前端PBKDF2)。
- 代码：`src/App.tsx`(总管state) / `components/`(UI) / `lib/`(逻辑:NLP/加解密/mascotBrain/bloub/petPhysics) / `worker/`(后端+D1+migrations)。
- 桌宠：`PetShell.tsx`(交互壳, 有甩飞/拖拽/pointercancel陷阱—修过) `MascotAssistant.tsx`(聊天, 离题拦截/授权/两段情绪) `lib/mascotBrain.ts`(本地小脑规则) `lib/bloub/`(变形引擎MIT)。
- 备忘录卡片：`MemoList.tsx`(memo-card/memo-flow) 编辑=居中`.drawer`。
- 常用命令：`npx tsc --noEmit`+`npm run build`→`cd worker && CLOUDFLARE_API_TOKEN=... wrangler deploy`；GitHub push 需代理 `HTTPS_PROXY=http://127.0.0.1:7897`。
- 部署 token 见记忆(别硬编码)；git remote 内嵌 GitHub PAT 待轮换。

## 6. 文档地图

| 文件 | 作用 |
|---|---|
| `_devlog/00-需求与状态跟踪.md` | **唯一权威"现在到哪了"**(实时更新) |
| `_devlog/01-v38继续执行.md` | v3.8 逐组技术细节 |
| `_devlog/02-框架与安全清单.md` | 架构/组件/凭据安全 |
| `_devlog/03-竞品调研笔记.md` | 四线调研精华(卡片/情绪/桌宠/任务+引导) |
| `_devlog/04-商业化路线图.md` | P0~P4 盈利路线 |
| 桌面 `轻待办-需求与状态跟踪.md` / `轻待办-项目记录.md` | 给用户看的副本 |

## 8. 技能路由表（大脑判断 → 调对应 skill，别凭感觉硬来）

> 用户明确要求：Claude 要**自己根据需求判断调哪个技能**，不要只会装不会用。看到需求先查这张表。
> 可用技能清单以当时会话能调的为准（如 `frontend-design`、`simplify`、`review`、`dataviz`、竞品分析、PRD 等）。

| 用户需求类型 | 判断信号 | 调用的技能/流程 |
|---|---|---|
| "太丑/难看/太平淡/没设计感" | 任何 UI/视觉抱怨 | **先调 frontend-design**（按它的流程：先出设计计划再写码），**再看真实成品案例**再改，绝不瞎调 CSS |
| "看一下同类/别人怎么做的/去查 XX" | 竞品/同类调研 | 调 **competitive-analysis**（先跟你确认范围再搜，禁止乱猜） |
| "我想要 X 功能"（模糊想法） | 需求不清晰、要落地 | 调 **prd-generator**（交互式问清→结构化需求→再动手） |
| "卡住了/很慢/出 bug" | 疑难 bug | 走 **systematic-debugging / superpowers** 流程（先定位根因，别猜） |
| "改了一堆没变化/白做" | 无效功反复 | 停手 → 回到 frontend-design 看真实参考 + 问清再动 |
| 代码写完了要收质量 | 需要清理/复盘 | 调 **simplify / review** |
| 记录/文档同步 | 每轮对话收尾 | 更新 `_devlog/00` + 桌面 + GitHub（无技能，纯纪律） |

**铁律**：用户抱怨 UI = 默认先走 frontend-design + 真实案例，不直接写 CSS。这条写进记忆最优先级。

## 8.5 本地技能/参考库（已建，按需调用，别只装不用）

**装在哪（都已就位）：**
- 设计改版流程方法论：**impeccable**（用户级 `~/.claude/skills/impeccable/`，系统已识别可调，用 `/` 或需求命中时触发）→ "改/审/打磨/audit/critique 界面"时用
- 真实成品案例库（35 种风格成品 HTML+说明）：`C:/Users/L/.claude/reference/web-templates/templates/`（如 Glassmorphism/Neo-Brutalism/Minimal-Dark/SaaS…各含 .html 成品+.md 说明）
- 品牌设计 token（54 个真实品牌：Stripe/Linear/Claude/Notion/Apple/Figma…精确色板/字号/间距）：`C:/Users/L/.claude/reference/brand-design-tokens/templates/*.md`
- 竞品分析 + PRD：项目级 `.claude/skills/{competitive-analysis,prd-generator}`
- superpowers 系列：用户级（brainstorming/writing-plans/systematic-debugging/TDD 等）

**怎么调（大脑判断→先查这）：**
- "登录页丑 / 卡片难看 / 没设计感" → 先 frontend-design（设计计划）→ 再 **impeccable**（critique/audit 现有界面）→ 翻 **web-templates** 找参考风格 → 翻 **brand-design-tokens** 取精确 token → 最后才写码
- 想让某块"更精致/更专业" → impeccable 的 polish/bolder 等 40 个手册
- 记住：这些是**工具**，根因是"改 UI 前必须看参考"，别再凭空写 CSS。

## 7. 调研精华(一句话版, 避免重复查)

- **flomo/memos(卡片笔记)**：核心=先无脑收下再回顾；"每日回顾/随机翻旧卡撞见过去的自己"是最大卖点。不做文件夹树。
- **情绪陪伴**：顺序纪律(先问清→镜映感受→安慰→建议最后)；精确点情绪名+复述原词比"我懂你"有效；危机词本地硬gate。
- **桌宠(dsh-pet-whale等)**：让宠物跟随用户真实行为反应(打勾庆祝/久不用打瞌睡)+亲密度分档+情感注入AI system+小破绽最出血肉感。
- **任务管理**：NLP识别实时高亮成可点改chips；完成要有仪式(音效+点亮+鼓励)；空状态=最高杠杆界面(一句用途+建第一个按钮+示范)；"今日=亲手挑"配建议灯泡。
- (详细见 03，别再整个重查)

---

*维护：每次对话结束更新。CLAUDE.md 含项目决策非机密；勿放 token/secret。*
